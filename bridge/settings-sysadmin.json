{
  "_comment": "Settings ТОЛЬКО для Сисадмина. Он управляет системой, поэтому ему разрешён доступ к bridge/.env (прописать API-ключи). Остальные агенты используют settings.json, где .env заблокирован. install.sh заменяет __AIOS_ROOT__.",
  "cacheRetention": "long",
  "permissions": {
    "bypass": true,
    "blockedCommands": [
      "rm -rf /",
      "rm -rf ~",
      "rm -rf /home",
      "rm -rf /root",
      "rm -rf __AIOS_ROOT__",
      "dd if=",
      "mkfs",
      "mkfs.ext4",
      "mkfs.xfs",
      "fdisk",
      "parted",
      "shred",
      ":(){ :|:& };:",
      "chmod -R 000",
      "chown -R nobody",
      "> /dev/sda",
      "> /dev/nvme",
      "curl * | sh",
      "curl * | bash",
      "wget * | sh",
      "wget * | bash",
      "shutdown",
      "reboot",
      "halt",
      "systemctl disable ssh",
      "systemctl stop ssh",
      "ufw disable",
      "iptables -F",
      "passwd",
      "userdel",
      "groupdel"
    ],
    "blockedPaths": [
      "/etc/shadow",
      "/etc/passwd",
      "/etc/sudoers",
      "~/.ssh",
      "~/.claude/credentials.json"
    ],
    "requireApproval": [
      "git push --force",
      "systemctl stop",
      "systemctl disable"
    ]
  }
}
